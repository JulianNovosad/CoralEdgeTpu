
---

# **🔥 Minimal Coral / Edge TPU Debug Log Template (Short & Practical)**

## **1 — Basic info**

* **Date:**
* **Short title:**
* **Purpose:**
* **Device:** (Raspberry Pi… etc.)
* **Model file:** (`name.tflite`, sha256 optional)
* **Delegate:** (`edgetpu` / CPU)

---

## **2 — System snapshot (paste outputs)**

### **OS & kernel**

```
uname -a
cat /etc/os-release
```

### **Hardware**

```
lscpu
free -h
lsusb
```

### **Edge TPU runtime**

```
dpkg -l | grep edgetpu
dmesg | grep -i edgetpu
```

---

## **3 — Build info**

* **Build command used:**
* **Toolchain versions (paste):**

```
g++ --version
cmake --version
```

---

## **4 — Run info**

### **Exact run command**

```
<copy/paste full command>
```

### **Observed behavior**

* **Symptoms:** (crash? “No such file”? wrong output?)
* **Terminal output:**
* **Severity:** minor / major / blocker

---

## **5 — Minimal repro steps**

Write the **3–6 exact steps** needed to reproduce the issue.

---

## **6 — Diagnostics done so far**

Paste **only what matters**, e.g.:

```
ldd ./your_binary
strace -e file ./your_binary
gdb --args ./your_binary
```

---

## **7 — Current hypothesis**

Short 1–2 sentences:
*e.g.*, "Probably linking against wrong libtensorflow-lite.so".

---

## **8 — Resolution / current status**

* What fixed it OR
* What still blocks it

---

# **Optional quick-collection script**

You can attach this if needed:

```bash
TS=$(date -u +%Y%m%dT%H%M%SZ)
OUT=log-$TS
mkdir -p $OUT
uname -a > $OUT/uname.txt
cat /etc/os-release > $OUT/os-release.txt
lscpu > $OUT/lscpu.txt
lsusb > $OUT/lsusb.txt
dmesg | tail -n 200 > $OUT/dmesg.txt
ldd ./coral_test > $OUT/ldd.txt
sha256sum model.tflite > $OUT/model.sha256
tar czf $OUT.tar.gz $OUT
```

---
